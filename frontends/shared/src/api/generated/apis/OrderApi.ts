/* tslint:disable */
/* eslint-disable */
/**
 * Surplasse API
 * Surplasse lets independent restaurants run their own direct ordering channel. This contract is the single source of truth of the API: the backend implements Java interfaces generated from it, the frontends consume a generated TypeScript client.  Version 0.1 covers the public catalog, ordering, payment, restaurateur identity flows, the authenticated operational-order list and the order-intake control. Other Dashboard reads remain drafts (`x-draft` blocks, excluded from generation and compatibility checks) until their authorization is implemented end to end.  Conventions: paths are prefixed with `/v1`, money amounts are integers in cents with a `Cents` suffix, timestamps are ISO 8601 UTC, and every error is an RFC 9457 Problem Details document.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  Order,
  OrderCreationRequest,
  OrderPage,
  OrderStatusResult,
  OrderStatusUpdate,
  Problem,
  TableSession,
  TableSessionRequest,
} from '../models/index';

export interface CreateOrderRequest {
    idempotencyKey: string;
    orderCreationRequest: OrderCreationRequest;
}

export interface CreateTableSessionRequest {
    tableSessionRequest: TableSessionRequest;
}

export interface GetOrderRequest {
    orderId: string;
    trackingToken: string;
}

export interface ListOrdersRequest {
    establishmentId: string;
    cursor?: string;
    limit?: number;
}

export interface UpdateOrderStatusRequest {
    orderId: string;
    orderStatusUpdate: OrderStatusUpdate;
}

/**
 *
 */
export class OrderApi extends runtime.BaseAPI {

    /**
     * Creates an order at status `pending_payment` from the cart validated by the customer. The cart itself never exists server side. The establishment and the table come exclusively from the anonymous table session (a customer can only order for the table whose QR was actually scanned). The server recomputes every amount from the catalog; amounts are never accepted from the client. The `Idempotency-Key` header guarantees that an unstable connection can never create the same order twice. Only `on_site` orders are accepted at this stage; `takeaway` is documented for the target and rejected with a business rule error until it opens. An order-intake pause rejects every new intention with `order-intake-paused`, while an exact replay still returns the order created before the pause.
     * Create an order from a validated cart
     */
    async createOrderRaw(requestParameters: CreateOrderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Order>> {
        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling createOrder().'
            );
        }

        if (requestParameters['orderCreationRequest'] == null) {
            throw new runtime.RequiredError(
                'orderCreationRequest',
                'Required parameter "orderCreationRequest" was null or undefined when calling createOrder().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Table-Session"] = await this.configuration.apiKey("X-Table-Session"); // tableSession authentication
        }


        let urlPath = `/v1/orders`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['orderCreationRequest'],
        }, initOverrides);

        return new runtime.JSONApiResponse(response);
    }

    /**
     * Creates an order at status `pending_payment` from the cart validated by the customer. The cart itself never exists server side. The establishment and the table come exclusively from the anonymous table session (a customer can only order for the table whose QR was actually scanned). The server recomputes every amount from the catalog; amounts are never accepted from the client. The `Idempotency-Key` header guarantees that an unstable connection can never create the same order twice. Only `on_site` orders are accepted at this stage; `takeaway` is documented for the target and rejected with a business rule error until it opens. An order-intake pause rejects every new intention with `order-intake-paused`, while an exact replay still returns the order created before the pause.
     * Create an order from a validated cart
     */
    async createOrder(requestParameters: CreateOrderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Order> {
        const response = await this.createOrderRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Exchanges the table code carried by a scanned QR (`{slug}.surplasse.com/?table={code}`) for an opaque anonymous session token, bound to the establishment and to that table. The token authorizes the ordering endpoints (`X-Table-Session` header) for the duration of a meal (2 hours, sliding while active). It carries no personal data. An unknown slug, an unknown or inactive table code, an establishment that is not active or an operational order-intake pause all yield a 404 without distinction.
     * Open an anonymous table session from a scanned QR code
     */
    async createTableSessionRaw(requestParameters: CreateTableSessionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TableSession>> {
        if (requestParameters['tableSessionRequest'] == null) {
            throw new runtime.RequiredError(
                'tableSessionRequest',
                'Required parameter "tableSessionRequest" was null or undefined when calling createTableSession().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';


        let urlPath = `/v1/table-sessions`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['tableSessionRequest'],
        }, initOverrides);

        return new runtime.JSONApiResponse(response);
    }

    /**
     * Exchanges the table code carried by a scanned QR (`{slug}.surplasse.com/?table={code}`) for an opaque anonymous session token, bound to the establishment and to that table. The token authorizes the ordering endpoints (`X-Table-Session` header) for the duration of a meal (2 hours, sliding while active). It carries no personal data. An unknown slug, an unknown or inactive table code, an establishment that is not active or an operational order-intake pause all yield a 404 without distinction.
     * Open an anonymous table session from a scanned QR code
     */
    async createTableSession(requestParameters: CreateTableSessionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TableSession> {
        const response = await this.createTableSessionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns an order for the customer tracking page. Access is authorized by the order tracking token (a non-guessable capability returned at creation, carried by the tracking page URL): the customer can close the browser and come back. A wrong token yields a 404 without distinction.
     * Get an order for its tracking page
     */
    async getOrderRaw(requestParameters: GetOrderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Order>> {
        if (requestParameters['orderId'] == null) {
            throw new runtime.RequiredError(
                'orderId',
                'Required parameter "orderId" was null or undefined when calling getOrder().'
            );
        }

        if (requestParameters['trackingToken'] == null) {
            throw new runtime.RequiredError(
                'trackingToken',
                'Required parameter "trackingToken" was null or undefined when calling getOrder().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['trackingToken'] != null) {
            queryParameters['trackingToken'] = requestParameters['trackingToken'];
        }

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/v1/orders/{orderId}`;
        urlPath = urlPath.replace(`{${"orderId"}}`, encodeURIComponent(String(requestParameters['orderId'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response);
    }

    /**
     * Returns an order for the customer tracking page. Access is authorized by the order tracking token (a non-guessable capability returned at creation, carried by the tracking page URL): the customer can close the browser and come back. A wrong token yields a 404 without distinction.
     * Get an order for its tracking page
     */
    async getOrder(requestParameters: GetOrderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Order> {
        const response = await this.getOrderRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Cursor-paginated list of the operational orders of one establishment for the Dashboard, sorted by creation timestamp and identifier, both descending. Only `paid`, `accepted`, `preparing` and `ready` orders are returned. Pending payment and terminal orders are deliberately excluded from this phase.  The opaque cursor stays stable while new orders arrive and is never interpreted by the client. The restaurateur session cookie and establishment membership are both required: an unknown establishment and an establishment outside the caller\'s scope yield the same 404.
     * List the operational orders of an establishment
     */
    async listOrdersRaw(requestParameters: ListOrdersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OrderPage>> {
        if (requestParameters['establishmentId'] == null) {
            throw new runtime.RequiredError(
                'establishmentId',
                'Required parameter "establishmentId" was null or undefined when calling listOrders().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['establishmentId'] != null) {
            queryParameters['establishmentId'] = requestParameters['establishmentId'];
        }

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/v1/orders`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response);
    }

    /**
     * Cursor-paginated list of the operational orders of one establishment for the Dashboard, sorted by creation timestamp and identifier, both descending. Only `paid`, `accepted`, `preparing` and `ready` orders are returned. Pending payment and terminal orders are deliberately excluded from this phase.  The opaque cursor stays stable while new orders arrive and is never interpreted by the client. The restaurateur session cookie and establishment membership are both required: an unknown establishment and an establishment outside the caller\'s scope yield the same 404.
     * List the operational orders of an establishment
     */
    async listOrders(requestParameters: ListOrdersRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OrderPage> {
        const response = await this.listOrdersRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Advances one order on behalf of an authenticated restaurateur. The target must be the next state allowed by the order state machine: `paid` to `accepted`, `accepted` to `preparing`, `preparing` to `ready`, then `ready` to `served` for an on-site order or `picked_up` for a takeaway order. Repeating the state already reached is idempotent and returns the current result without emitting another event. Refunds are deliberately excluded: they require a payment operation and cannot be represented by a status-only update.  The restaurateur session cookie is required. Unknown orders and orders outside the caller\'s establishment scope yield the same 404.
     * Advance an order through the operational workflow
     */
    async updateOrderStatusRaw(requestParameters: UpdateOrderStatusRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OrderStatusResult>> {
        if (requestParameters['orderId'] == null) {
            throw new runtime.RequiredError(
                'orderId',
                'Required parameter "orderId" was null or undefined when calling updateOrderStatus().'
            );
        }

        if (requestParameters['orderStatusUpdate'] == null) {
            throw new runtime.RequiredError(
                'orderStatusUpdate',
                'Required parameter "orderStatusUpdate" was null or undefined when calling updateOrderStatus().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';


        let urlPath = `/v1/orders/{orderId}/status`;
        urlPath = urlPath.replace(`{${"orderId"}}`, encodeURIComponent(String(requestParameters['orderId'])));

        const response = await this.request({
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['orderStatusUpdate'],
        }, initOverrides);

        return new runtime.JSONApiResponse(response);
    }

    /**
     * Advances one order on behalf of an authenticated restaurateur. The target must be the next state allowed by the order state machine: `paid` to `accepted`, `accepted` to `preparing`, `preparing` to `ready`, then `ready` to `served` for an on-site order or `picked_up` for a takeaway order. Repeating the state already reached is idempotent and returns the current result without emitting another event. Refunds are deliberately excluded: they require a payment operation and cannot be represented by a status-only update.  The restaurateur session cookie is required. Unknown orders and orders outside the caller\'s establishment scope yield the same 404.
     * Advance an order through the operational workflow
     */
    async updateOrderStatus(requestParameters: UpdateOrderStatusRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OrderStatusResult> {
        const response = await this.updateOrderStatusRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
