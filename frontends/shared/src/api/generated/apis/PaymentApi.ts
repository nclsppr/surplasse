/* tslint:disable */
/* eslint-disable */
/**
 * Surplasse API
 * Surplasse lets independent restaurants run their own direct ordering channel. This contract is the single source of truth of the API: the backend implements Java interfaces generated from it, the frontends consume a generated TypeScript client.  Version 0.1 covers the public catalog, ordering, payment, restaurateur identity flows, the authenticated operational-order list and the order-intake control. Other Dashboard reads remain drafts (`x-draft` blocks, excluded from generation and compatibility checks) until their authorization is implemented end to end.  Conventions: paths are prefixed with `/v1`, money amounts are integers in cents with a `Cents` suffix, timestamps are ISO 8601 UTC, and every error is an RFC 9457 Problem Details document.
 *
 * The version of the OpenAPI document: 0.1.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  PaymentCreationRequest,
  PaymentSession,
  Problem,
  Refund,
  RefundCreationRequest,
} from '../models/index';

export interface CreatePaymentRequest {
    idempotencyKey: string;
    paymentCreationRequest: PaymentCreationRequest;
}

export interface CreateRefundRequest {
    idempotencyKey: string;
    refundCreationRequest: RefundCreationRequest;
}

/**
 *
 */
export class PaymentApi extends runtime.BaseAPI {

    /**
     * Creates (or returns, when replayed) the Stripe payment session of a `pending_payment` order: the backend re-checks product availability, recomputes the amount from the catalog, creates a direct-charge PaymentIntent on the establishment\'s connected account and returns its client secret and account identifier for the Payment Element. The browser confirmation is informative only: the signed Stripe webhook is the single source of truth that marks the order paid. An operational pause rejects new and replayed payment-session requests but never prevents a signed webhook from reconciling a Payment Intent already in flight.
     * Create the payment session of an order
     */
    async createPaymentRaw(requestParameters: CreatePaymentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PaymentSession>> {
        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling createPayment().'
            );
        }

        if (requestParameters['paymentCreationRequest'] == null) {
            throw new runtime.RequiredError(
                'paymentCreationRequest',
                'Required parameter "paymentCreationRequest" was null or undefined when calling createPayment().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Table-Session"] = await this.configuration.apiKey("X-Table-Session"); // tableSession authentication
        }


        let urlPath = `/v1/payments`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['paymentCreationRequest'],
        }, initOverrides);

        return new runtime.JSONApiResponse(response);
    }

    /**
     * Creates (or returns, when replayed) the Stripe payment session of a `pending_payment` order: the backend re-checks product availability, recomputes the amount from the catalog, creates a direct-charge PaymentIntent on the establishment\'s connected account and returns its client secret and account identifier for the Payment Element. The browser confirmation is informative only: the signed Stripe webhook is the single source of truth that marks the order paid. An operational pause rejects new and replayed payment-session requests but never prevents a signed webhook from reconciling a Payment Intent already in flight.
     * Create the payment session of an order
     */
    async createPayment(requestParameters: CreatePaymentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PaymentSession> {
        const response = await this.createPaymentRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates or replays the full Stripe refund of one order on behalf of authenticated establishment staff. The direct charge is refunded on the connected account frozen on the payment. When Surplasse collected an application fee, the same operation returns it to the restaurant. Partial refunds are outside the MVP.  The request is idempotent. Replaying a key returns the exact refund it previously received, while a new key reuses any refund still in progress or already succeeded. A failed or canceled attempt can be retried with a new key. The order moves to `refunded` only after Stripe reports a successful refund, synchronously or through a signed webhook.
     * Fully refund a paid order
     */
    async createRefundRaw(requestParameters: CreateRefundRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Refund>> {
        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling createRefund().'
            );
        }

        if (requestParameters['refundCreationRequest'] == null) {
            throw new runtime.RequiredError(
                'refundCreationRequest',
                'Required parameter "refundCreationRequest" was null or undefined when calling createRefund().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }


        let urlPath = `/v1/refunds`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['refundCreationRequest'],
        }, initOverrides);

        return new runtime.JSONApiResponse(response);
    }

    /**
     * Creates or replays the full Stripe refund of one order on behalf of authenticated establishment staff. The direct charge is refunded on the connected account frozen on the payment. When Surplasse collected an application fee, the same operation returns it to the restaurant. Partial refunds are outside the MVP.  The request is idempotent. Replaying a key returns the exact refund it previously received, while a new key reuses any refund still in progress or already succeeded. A failed or canceled attempt can be retried with a new key. The order moves to `refunded` only after Stripe reports a successful refund, synchronously or through a signed webhook.
     * Fully refund a paid order
     */
    async createRefund(requestParameters: CreateRefundRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Refund> {
        const response = await this.createRefundRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
